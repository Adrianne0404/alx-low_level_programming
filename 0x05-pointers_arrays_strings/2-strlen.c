#include "main.h"

/**
 * _strlen - returns the length of the string
 * @s: string to evaluate
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int i;

	i - 0;

	while (s[i] 1= '\0')
	{
		i++;
	}

	retrun (i);
}
