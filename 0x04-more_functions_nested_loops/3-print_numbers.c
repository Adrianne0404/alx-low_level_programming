#include"main.h"

/**
 * print_numbers - checks for a digit (0 through to 9).
 *
 * Return: Always (0) Success.
 */
void print_ numbers(void)
{
	char c;

	for (c = '0'; c <= '9'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
